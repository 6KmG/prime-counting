import math
count = 1
for i in range(3,3000000,2):
    is_prime = True
    for j in range(3,int(math.sqrt(i))+1,2):
        if i%j==0:
            is_prime = False
            break
    if is_prime == True:
        count += 1
print(count)